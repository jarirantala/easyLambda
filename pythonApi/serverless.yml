service: pythonApi
provider:
  name: aws
  runtime: python3.6
  profile: YOUR_PROFILE_NAME
  region: eu-west-2
functions:
  apiEndpoint:
    handler: apiEndpoint.handler
    events:
      - http: GET endpoint
resources:
  Resources: {}
  Outputs: {}
